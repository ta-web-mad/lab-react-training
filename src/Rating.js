import React from 'react'


const Rating = (props) => {

    const batman = props => {
        console.log (Math.floor(props))
        return Math.floor(props)
    }

  return (
      <div>☆ </div>
     
   
  )
}

export default Rating


// ☆☆☆☆☆

// ★★★★★